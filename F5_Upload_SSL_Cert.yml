---
- name: upload ssl cert
  hosts: all
  connection: local
  gather_facts: no

  tasks: 
  - debug: msg="{{lookup('file', '/lib_core/cert_1.pem') }}"

  - name: Import PEM Certificate
    bigip_ssl_certificate:
      name: cert_1.pem
      state: present
      content: "{{lookup('file', '/lib_core/cert_1.pem') }}"
      provider:
        server_port: 443
        server: 192.168.201.251
        validate_certs: no
    delegate_to: localhost
